import { GenreValidDirective } from './genre-valid.directive';

describe('GenreValidDirective', () => {
  it('should create an instance', () => {
    const directive = new GenreValidDirective();
    expect(directive).toBeTruthy();
  });
});
