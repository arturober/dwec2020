import { ForGroupDirective } from './for-group.directive';

describe('ForGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new ForGroupDirective();
    expect(directive).toBeTruthy();
  });
});
